export const scrollToHome = () => {
  window.scrollTo({
    top: 0,
  });
};
export const scrollToTechStack = () => {
  window.scrollTo({
    top: 200,
  });
};
export const scrollToProject = () => {
  window.scrollTo({
    top: 1000,
  });
};
